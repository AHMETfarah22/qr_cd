import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF051208); // Deep Dark Green
  static const Color process = Color(0xFF0F2615); // Lighter Green for cards/inputs
  static const Color accent = Color(0xFF2BFF00); // Neon Green
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF88A88A); // Muted Greenish Grey
  static const Color error = Color(0xFFFF3B3B);
}
